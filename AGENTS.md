# CodeMate 项目协作规则

## 代码修改流程

- 所有代码或配置文件修改都必须经过三个步骤：
  1. 编写代码。
  2. 代码审查：检查改动范围、边界条件、兼容性和数据安全；必要时使用 code-review 技能。
  3. 代码测试：优先运行与改动直接相关的测试，再根据影响范围运行更完整的测试。
- 未完成代码审查，不得直接进入测试或向用户宣称改动完成。
- 较小规模的改动也必须执行审查；可以采用简短的自审，但要明确检查结果。
- 不修改与当前任务无关的代码；发现已有问题时单独说明，不顺手修复。

## 文档编写规则

- 用户没有指定位置时，先寻找合适的位置，优先考虑 local_docs。
- 用户没有指定语言时，默认使用中文。
- 文档大纲未确认时，先形成大纲再编写正文。
- 文档要易读，避免大篇代码；算法流程用文字说明，数据模型优先使用表格展示。
